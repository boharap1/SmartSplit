# SmartSplit Cloud Functions — local environment variables
# DO NOT commit this file (.gitignore should exclude it).
#
# Gmail setup:
#   1. Google Account → Security → 2-Step Verification (enable it)
#   2. Security → App passwords → "Other (Custom name)" → "SmartSplit"
#   3. Copy the 16-char password (spaces are OK — Gmail ignores them)
#
# For production: set these in Firebase Console →
#   Functions → Configuration → Environment variables

EMAIL_USER=boharaprakash4444@gmail.com
EMAIL_PASS=fctd zypy kelr wioz
